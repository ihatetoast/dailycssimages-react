import React, { Component, Fragment } from "react";

class CardGrid extends Component {
  constructor(props) {
    super(props);
    this.state = {
      renderedCards: []
    };
  }

  //render the cards from the json imported
  //grid will be reusd in css and svg. the card comp will also bethe same. the only diff will be content of the card.

  //props for this card grid will change dep on the parent component used
}

export default CardGrid;
